# For more options and information see
# http://rptl.io/configtxt
# Some settings may impact device functionality. See link above for details

# Uncomment some or all of these to enable the optional hardware interfaces
#dtparam=i2c_arm=on
#dtparam=i2s=on
#dtparam=spi=on

# Disable Bluetooth to save power and reduce interference
dtoverlay=disable-bt

# Disable onboard audio processing
dtparam=audio=off

# Disable automatic detection of Camera and Display (CSI/DSI) ports
camera_auto_detect=0
display_auto_detect=0

# Disable HDMI output and ignore display hotplugging to save energy
dtparam=hdmi=off

# Additional overlays and parameters are documented
# /boot/firmware/overlays/README

# Automatically load initramfs files, if found
auto_initramfs=1

# Don't have the firmware create an initial video= setting in cmdline.txt.
# Use the kernel's default instead.
disable_fw_kms_setup=1

# Allocate minimum memory to the GPU to maximize RAM available for k3s nodes
gpu_mem=16

# CPU and Architecture
arm_64bit={{ rpi_arm_64bit }}
arm_boost={{ rpi_arm_boost }}

# Fan control settings
dtparam=fan_temp0={{ rpi_fan_temp0 }}
dtparam=fan_temp0_hyst={{ rpi_fan_temp0_hyst }}
dtparam=fan_temp1={{ rpi_fan_temp1 }}
dtparam=fan_temp1_hyst={{ rpi_fan_temp1_hyst }}

# Disable the green activity LED
dtparam=act_led_trigger=none
dtparam=act_led_activelow=off

# Disable the red power LED as well
dtparam=pwr_led_trigger=none
dtparam=pwr_led_activelow=off

# Enable PCIe Gen 3 for maximum NVMe speed
dtparam=pciex1_gen=3

# Increase timeout for NVMe drives to ensure stable boot
boot_delay=1

# Disable compensation for displays with overscan
disable_overscan=1
